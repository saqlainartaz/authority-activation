# Stakeholder integration goal and continuation

Date: 2026-09-14. Status: historical starting record; its blocked/completion wording and requirement to retain the old admin appearance were superseded by the operator's later outcome and admin-redesign approval. Current implementation evidence is in `CONTINUATION.md` and `VERIFICATION.md`.

## Operator instruction and authority

The operator requested a goal for connecting the newly designed frontend to the previously working application stack, retaining the existing admin dashboard and all its functionality, preserving the new client design exactly, and testing everything end to end. The stakeholder work must remain separate from the engine overhaul, be ignored by that repository, and belong to the new GitHub repository. The operator then supplied https://github.com/saqlainartaz/authority-activation-frontend.

Ordinary local investigation, integration, fixes and verification are within that outcome. Preserve the original engine checkout, its uncommitted work, Final Front End, and the separate overhaul worktree and services. There is no authority here for commits, pushes, PRs, deployment, shared/retained database use, private sources, paid provider calls, or reading/renaming .env files. Cycle 2's service and spending grants do not transfer to this project. Obtain specific missing resource authority when needed; continue independent work meanwhile.

## Goal prompt

Fully integrate this repository's approved frontend with the verified previously working pre-overhaul backend and TypeScript server agent for stakeholder use. Retain the existing Final Front End admin dashboard, its appearance, authorization and every functional workflow. Transfer all verified previous client capabilities to the new design without silent omissions.

Preserve the design contract in DESIGN-CONTRACT.md and the behavior in DESIGN-AND-BEHAVIOR.md: existing components, themes, typography, spacing, navigation, action placement, responsive thresholds and onboarding progression. Replace fixtures, simulated generation and browser-only application persistence with actual services through adapters. Real data and necessary pending/error/retry states may change content while preserving composition. Do not replace the chosen libraries or redesign screens.

First identify and pin the compatible old frontend/backend revisions using code, contracts and dated execution evidence. Do not assume the current engine HEAD is the correct old backend. Inventory each previous admin/client capability and each actionable new-design control; map its actual API, persistence, jobs, agent behavior and acceptance evidence. Report unsupported capabilities and conflicts without fabricating endpoints, hiding controls or returning fake success. Resolve ordinary mapping problems locally; obtain a product decision for a material capability or visual/flow exception.

Work only in this independent stakeholder repository and task-owned isolated old-backend resources. All stakeholder changes must stay ignored by the overhaul repository. Never connect to or import the latest overhaul engine. Preserve all overhaul code, plans, data, services and progress. The new frontend repository is the intended delivery repository; publication requires explicit authority.

Connect session restoration, invitations, login/logout, user and tenant scoping, onboarding, profile/settings, training/guidance, source intake and processing, citations, agent streaming/cancellation, drafts/versions, review/approval, library and scheduling wherever those capabilities exist in the verified old application. Map extra new-design requirements explicitly. Distinguish scheduling from actual external publishing. Keep marketing-copy generation in the TypeScript server agent; Python supplies structured knowledge. Keep provider and service credentials server-only.

Use meaningful integration and browser end-to-end tests against the isolated old application stack with synthetic data and an explicitly authorized disposable database. Verify persistence across reloads/sessions, administrative access, cross-tenant denial, direct links, async status, retries/cancellation, draft evidence and timezone/DST scheduling. Compare client and admin screenshots at representative desktop, tablet and phone widths, including the 768px and 1180px boundaries and light/dark modes. Do not treat the existing design tests or a production build as connected-service evidence.

Use deterministic provider substitutes for keyless checks and state their limitations. Live provider/model validation, actual emails or social publishing, private data and external costs need their own authority; no successful mock proves a live service works.

Continue authorized investigation, implementation, debugging and verification across checkpoints and restarts without repeated permission requests. Respect applicable model-routing and architecture requirements. Preserve a compact progress record here; leave the overhaul's CYCLE and plans untouched. Completion requires every required workflow and actionable control connected and verified, design fidelity demonstrated, repository/runtime isolation checked, reproducible setup documented and no unresolved required capability or verification gap. Report exact code states, checks, outcomes, remaining simulated paths, limitations and approved exceptions honestly.

## Verified starting state

- Destination: https://github.com/saqlainartaz/authority-activation-frontend.
- Initial destination HEAD: `ec2f05f83dd70b59cc39d3d2240fad9b291d8d17` (Deliver approved frontend with design-preserving integration handoff).
- Local clone: `C:/Users/saqla/Desktop/InsideSuccess/marketing tool/local/stakeholder-integration/frontend`.
- App lives in its nested `frontend/` directory and uses React/Vite, React Router, Base UI, assistant-ui and DayPicker. No Sites manifest is present in the tracked export.
- Old frontend reference: `C:/Users/saqla/Desktop/InsideSuccess/marketing tool/Final Front End`, clean at inspection, HEAD `8c790eb`, dated 2026-08-26. Its origin is `Inside-Success/ai-marketing-tool-mockup` and its stack is Next.js with server API handlers and a TypeScript agent.
- Old frontend contains `/internal` admin panels and internal API routes for clients, documents, people, onboarding tokens, held content, search, atoms and voice profiles. This is a file inventory, not proof every workflow currently passes.
- Old frontend README is stale relative to its connected routes and handoff. Verify actual callers and tests.
- Exact compatible old backend revision: not established yet. Date proximity alone is insufficient.
- `git check-ignore -v local/stakeholder-integration/frontend/` confirms exclusion through the original repository's existing `.gitignore` line 60 (`local/`). No ignore-file change was necessary.
- The original engine worktree already contains unrelated modified/untracked documents and prototype work. No original tracked file was edited by this task.
- New repository documentation reports 19 export logic tests and a build pass, plus owner-reported design browser testing. None is a new integration test result from this session.

## Next execution steps and constraints

1. Resume with Sol/high as execution lead. The original repository's `docs/AGENT_OPERATIONS.md`, Model routing and work limits, says: "If the lead is still Astra after this usage pause, finish the bounded configuration/handoff only; resume product execution in Sol/high rather than emulate a cheaper lead with nested agents." This turn is limited to that preparation/handoff. No delegation was launched.
2. Read this repository's AGENTS.md and its prescribed documents, then inspect the relevant old callers/backend contracts and narrow dated evidence to establish compatible revisions. Preserve original checkouts as references.
3. Produce the capability mapping and concrete integration architecture. Account for the new Vite client and old Next.js admin/server agent, including routing, same-origin authentication, CSS/theme isolation and deployment packaging. The new design is approved; do not reopen design exploration. Check any applicable source-repository architecture gate before modifying copied admin/server behavior.
4. Establish unchanged export checks and screenshot baselines, then implement coherent tested slices. Dependencies have not been installed and application tests have not been run in this clone.
5. Read the original test-isolation runbook before database operations. Do not run old `Final Front End/e2e/global-setup.ts` unchanged: it assumes localhost:5432/content_engine_e2e, runs migrations and seeds, and resolves the backend from a parent directory. Adapt the test harness to explicit task-owned isolated resources before execution.

The goal is incomplete and marked blocked after the same execution-model restriction remained across the initial preparation turn and two automatic continuations. The last continuation made no implementation progress; the rule was revalidated at docs/AGENT_OPERATIONS.md:59-60. Resume in Sol/high, or obtain an explicit operator exception authorizing Astra for this task. Automatic continuation alone does not override the restriction. No application source edits, backend checkout, DB operations, provider calls, commits or remote writes have occurred.
